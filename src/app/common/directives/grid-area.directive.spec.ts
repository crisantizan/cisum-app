import { GridAreaDirective } from './grid-area.directive';

describe('GridAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new GridAreaDirective();
    expect(directive).toBeTruthy();
  });
});
